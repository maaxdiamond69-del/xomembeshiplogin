services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python -m playwright install chromium
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
